Struktura každého adresáře v projektu je komplexní a nevešla by se do jednoho diagramu, proto zbytek popisu je v přidruženém souboru \texttt{README.md}.

\vspace{11pt}

\dirtree{%
    .1 /.
    .2 apps.
    .3 \textbf{docs} (zdrojový kód dokumentace).
    .4 \dots.
    .4 \textbf{README.md}.
    .3 \textbf{solid-start-example} (zdrojový kód demo aplikace).
    .4 \dots.
    .4 \textbf{README.md}.
    .2 packages.
    .3 \textbf{solid-apg} (zdrojový kód komponent).
    .4 \dots.
    .4 \textbf{README.md}.
    .3 \textbf{solid-aria} (git submodule).
    .2 \dots.
    .2 \textbf{README.md}.
}
